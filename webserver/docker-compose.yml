# Einfacher Webserver – absichtlich veraltete Version
# für Renovate-Demo (aktuelle Minor: 1.28.x)
services:
  nginx:
    image: nginx:1.30
    container_name: webserver
    ports:
      - "8080:80"
    volumes:
      - /opt/data/nginx/html:/usr/share/nginx/html:ro
    restart: unless-stopped
